version: 2.1
orbs:
  win: circleci/windows@2.4.1

jobs:
  build:
    executor: win/default
    steps:
      - checkout
      - run:
          name: Run unit tests
          command: |
            dotnet.exe --info
            cd tests\MySqlConnector.Tests
            dotnet.exe test
